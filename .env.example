# Copy to .env.local for local `node serve.mjs` (never commit .env.local).
# Vercel: Settings → Environment Variables → CAPABILITIES_ADMIN_SECRET
# (set for both Production and Preview if you use preview URLs; redeploy after saving)
# Optional alias: CAPABILITIES_ADMIN_PASSWORD
CAPABILITIES_ADMIN_SECRET=
