# Work — archived listings & experiments

## Current live work grid

**`/projects-v2/`** → `Work/unified/index.html` (baked catalog at build time).

Legacy listing URLs (`/work`, `/projects`, etc.) redirect to `/projects-v2/`. Project detail pages stay at `/work/{category}/{slug}/`.

## Archived here

| Path | Notes |
|------|--------|
| `index-redirect-to-projects-v2.html` | Former `Work/index.html` redirect stub (listing retired) |
| `unified-v3/` | Alternate grid experiment (`/projects-v3` redirected to v2) |
| `admin-redirect-to-adminv2.html` | Old `/work/admin` stub before unified admin at `/work/admin` |

## Admin

Unified site admin: **`/work/admin`** (`Work/admin.html`) — work CMS, home reel, capabilities allow list.
